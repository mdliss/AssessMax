# AWS Configuration
AWS_REGION=us-east-1
AWS_ACCOUNT_ID=000000000000

# AWS Cognito (not required for local testing)
COGNITO_USER_POOL_ID=us-east-1_local
COGNITO_CLIENT_ID=local_client_id
COGNITO_CLIENT_SECRET=local_client_secret
COGNITO_DOMAIN=local.auth.domain
COGNITO_JWKS_URL=http://localhost:4566/.well-known/jwks.json
JWT_AUDIENCE=local_audience

# Database (Local PostgreSQL)
DATABASE_URL=postgresql://max@localhost:5432/assessmax
DB_HOST=localhost
DB_PORT=5432
DB_NAME=assessmax
DB_USER=max
DB_PASSWORD=

# S3 Buckets (LocalStack)
S3_RAW_BUCKET=assessmax-raw
S3_NORMALIZED_BUCKET=assessmax-normalized
S3_OUTPUTS_BUCKET=assessmax-outputs

# DynamoDB Tables (LocalStack)
DYNAMODB_TABLE_CACHE=inference_cache
DYNAMODB_TABLE_PRESENCE=presence_tracking

# API Configuration
API_BASE_URL=http://localhost:8000
ALLOWED_ORIGINS=["http://localhost:3000","http://localhost:5173","http://localhost:8501"]

# File Upload Limits
MAX_FILE_SIZE_MB=25
MAX_CONCURRENT_UPLOADS=2

# Job Configuration
MAX_JOBS_PER_USER=1
JOB_TIMEOUT_SECONDS=300

# Security
JWT_SECRET_KEY=local_development_secret_key_not_for_production
JWT_ALGORITHM=HS256
JWT_EXPIRATION_MINUTES=60

# Environment
DEBUG=true
LOG_LEVEL=INFO

# LocalStack (for local AWS simulation)
LOCALSTACK_ENDPOINT=http://localhost:4566
AWS_ENDPOINT_URL=http://localhost:4566
BYPASS_AUTH=true

# AssessMax DynamoDB Tables
DYNAMO_JOBS_TABLE=dev-assessmax-jobs
DYNAMO_ARTIFACTS_TABLE=dev-assessmax-artifacts

# AssessMax S3 Buckets
S3_RAW_BUCKET=dev-assessmax-raw-971422717446
S3_NORMALIZED_BUCKET=dev-assessmax-normalized-971422717446
S3_OUTPUTS_BUCKET=dev-assessmax-outputs-971422717446
